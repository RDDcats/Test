네트워크 길찾기가 IP
전송되는 정보는 패킷(packet)

ISP 인터넷 서비스 프로바이더(skt)
라우터와 라우터 사이 거리 멈
멀면 아날로그 신호로 신호를 바꿔야함
광케이블을 많이씀

제일 목적지까지 빠르게 정보를 전송하는
라우팅 프로토콜, 라우팅 알고리즘, ip프로토콜
그물망이 있고 내 위치에서 가장가까운라우터에 연결한다
네트워크 장비하는 쪽은 ip를 한다 + 하드웨어에 돈많이 바른다

망은 잘되있고 ip주소만 넣으면 간다 이렇게 생각하면됨
망이 있다고 치고 기능을 만들고 싶다 > 어플리케이션 입장이다

어플리케이션은 send receive함수를 사용한다.

소켓lib - 전송하기위한 서비스 제공
소켓 (소켓 구조체) - 전송할 정보를 담는다. 매개체
tcp(전송 프로토콜) - 데이터 전송을 100% 보장
커널 - ?

운영체제가 죽으면 다죽기때문에
운영체제는 보완환경처럼 돌아간다
응용프로그램은 소켓라이브러리로 데이터를 전송하고
전송 프로토콜은 소켓에서 데이터를 가져다 실제로 데이터를 전송한다

전송할때 소켓을 생성해준다. 이때 네트워크 전송 프로토콜을 지정해야한다
TCP - 전송데이터를 복잡한방식으로 100%보장한다.
패킷을 복제해서 가지고있다가 전송이 실패하면 다시전송한다.
UDP - 전송데이터를 보장하지 않는다
소켓라이브러리로 설정을 바꿀수있다. 그럼 프로토콜이 다른식으로 작동한다
소켓을 만들면 고유id를 부여한다
고유 아이디로 데이터를 가져간다.

UDP는 한사람이 프로그램을 2개를 짜야댐
네트웤은 프로그램이 한개가아니라 2개씩이다.
전송 프로토콜이 뭐냐에 따라서 호출해야되는 함수가 다르다
통신 한쪽은 서버역할 정보를 제공해주는 역할
디비나 기능 빵빵하게
클라이언트는 컴퓨터가 작아 (portable) 배터리도 작아
자기가 연산량이 높거나 뭐 많이 하면안되
클라이언트는 서버에다 네트워크를 이용해서 요청을 전달.
서버는 요청을 처리, 가공하고 정보를 클라이언트에 전송
이게 클라이언트 서버 모델

tcp - 정보의 무결성 검사
1:1 재전송기능있음
손실없이 보내야되는 데이터 전송에 사용
udp - 무지성 송수신 
1:n 도 가능

소켓 생성할때 프로토콜 지정해줘야된다

